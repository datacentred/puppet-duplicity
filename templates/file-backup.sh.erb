#!/bin/bash
set -e
set -o pipefail

<% @_environment.each do |var|-%>
<%= var %>
<% end-%>
<%= @_pre_command %>
duplicity --verbosity warning --no-print-statistics --full-if-older-than <%= @_full_if_older_than -%> --s3-use-new-style <%= @_encryption -%><%= @_ssh_options -%><% _directories.each do |dir| %>--include '<%= dir -%>' <% end %>--exclude '**' / '<%= @_url -%>'<%= @_remove_older_than_command %>
<%= @_remove_older_than_command %>
<%= @_post_command %>
